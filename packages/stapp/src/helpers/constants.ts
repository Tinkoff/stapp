/**
 * @private
 */
export const APP_KEY = '@@stapp'

/**
 * @private
 */
export const COMPLETE = `${APP_KEY}/complete`

/**
 * @private
 */
export const SOURCE_MODULE = `${APP_KEY}/sourceModule`

/**
 * @private
 */
export const CHAIN_ID = `${APP_KEY}/chainId`
